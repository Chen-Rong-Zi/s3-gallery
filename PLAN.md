## Plan: Task 3.4 — File Detail Handler + Template

**Files:**
- `crates/ossgalley-web/src/handlers/file_detail.rs` (replace)
- `crates/ossgalley-web/templates/file_detail.html` (update)

**Steps:**
1. Write `file_detail.rs` handler — query file, metadata, thumbnail; group metadata by namespace; render template
2. Update `file_detail.html` — add thumbnail, namespace sections, human-readable size, type badge, content-type
3. Build and check compilation

**Estimate:** 15 min